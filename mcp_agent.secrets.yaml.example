# Example secrets file - copy to mcp_agent.secrets.yaml and fill in your values
# If using OpenAI models
openai:
  api_key: "your-openai-api-key-here"

# If using Anthropic models 
anthropic:
  api_key: "your-anthropic-api-key-here"

# Note: GitHub authentication is handled by the GitHub MCP server
# through the GitHub CLI or browser authentication